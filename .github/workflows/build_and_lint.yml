name: Build and Lint

on:
  push:
    branches:
      - main
  pull_request:
    igbranches-ignore:
      - main

jobs:
  setup_dependencies:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.8
      - name: Create virtual environment
        run: |
          python -m venv $HOME/.virtualenvs/platformio
          source $HOME/.virtualenvs/platformio/bin/activate
          python -m pip install --upgrade pip
          python -m pip install platformio
      - name: Activate virtual environment
        run: source $HOME/.virtualenvs/platformio/bin/activate
      - name: Cache virtual environment
        uses: actions/cache@v2
        with:
          path: $HOME/.virtualenvs/platformio
          key: ${{ runner.os }}-pio
          restore-keys: |
            ${{ runner.os }}-pio
  build:
    needs: setup_dependencies
    runs-on: ubuntu-latest
    steps:
      - name: Restore virtual environment cache
        uses: actions/cache@v2
        with:
          path: $HOME/.virtualenvs/platformio
          key: ${{ runner.os }}-pio
      - name: Activate virtual environment
        run: source $HOME/.virtualenvs/platformio/bin/activate
      - name: Run build
        run: pio run
  static_analysis:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Restore virtual environment cache
        uses: actions/cache@v2
        with:
          path: $HOME/.virtualenvs/platformio
          key: ${{ runner.os }}-pio
      - name: Activate virtual environment
        run: source $HOME/.virtualenvs/platformio/bin/activate
      - name: Run static analysis
        run: pio check --fail-on-defect high
  test:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Restore virtual environment cache
        uses: actions/cache@v2
        with:
          path: $HOME/.virtualenvs/platformio
          key: ${{ runner.os }}-pio
      - name: Activate virtual environment
        run: source $HOME/.virtualenvs/platformio/bin/activate
      - name: Run tests
        run: pio test
